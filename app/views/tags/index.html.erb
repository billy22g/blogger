<h1>All Tags</h1>

<ul>
<% @tags.each do |tag| %>
  <% if tag.articles.count != 0 %>
  <li><%= link_to tag.name, tag_path(tag) %><br/>
  <%= link_to "delete", tag_path(tag), method: :delete %></li><br/>
  <% end %>
<% end %>  
</ul>
